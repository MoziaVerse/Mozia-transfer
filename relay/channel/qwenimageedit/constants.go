package qwenimageedit

const (
	ChannelName = "qwenimageedit"
)

// Models exposed via this channel. The single canonical name pairs the
// Qwen-Image-Edit-2511 base with the fal Multiple-Angles + Lightning 8-step
// LoRAs that the upstream service has loaded.
var ModelList = []string{
	"qwen-image-edit-2511-multiple-angles",
}
